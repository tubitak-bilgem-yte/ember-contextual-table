{{yield (hash
  data=paginatedData
  currentPage=currentPage
  next=(action "next")
  previous=(action "previous")
  first=(action "first")
  last=(action "last")
  pagesTotal=pagesTotal
  hasNext=hasNext
  hasPrevious=hasPrevious
  )}}
