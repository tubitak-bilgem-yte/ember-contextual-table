{{#if (has-block-params)}}
  {{yield (hash change=(action "change") isSelected=(readonly isSelected) header=true)}}
{{else}}
  {{input type="checkbox" change=(action "change" value="target.checked") checked=(readonly isSelected)}}
{{/if}}